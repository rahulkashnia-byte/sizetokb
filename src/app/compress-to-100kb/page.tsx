"use client";

import { TargetKbLander } from "@/components/TargetKbLander";

export default function Page() {
  return (
    <TargetKbLander
      targetKb={100}
      titleAccent="100KB"
      path="/compress-to-100kb/"
      seoHeading="Compress image to 100KB online free"
      paragraphs={[
        "Search: compress image to 100KB, reduce photo size to 100KB for exams and ID uploads.",
      ]}
    />
  );
}
