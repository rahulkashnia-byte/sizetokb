"use client";

import { TargetKbLander } from "@/components/TargetKbLander";

export default function Page() {
  return (
    <TargetKbLander
      targetKb={10}
      titleAccent="10KB"
      path="/compress-to-10kb/"
      seoHeading="Compress image to 10KB online free"
      paragraphs={[
        "Search: compress image to 10KB, reduce photo size to 10KB — common for tight signature / thumb / portal limits.",
      ]}
    />
  );
}
